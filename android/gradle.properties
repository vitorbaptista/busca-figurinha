org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# AGP 8.9 needs JDK 17+; pin the mise-installed JDK 21 (the shimmed JDK 26 is too new for AGP).
# Adjust the path if your mise java install differs (`ls ~/.local/share/mise/installs/java`).
org.gradle.java.home=/home/vitor/.local/share/mise/installs/java/21
