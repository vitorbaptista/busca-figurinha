node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
dev-dist

# Local OCR test fixtures (sample sticker backs) — served only in dev, never shipped.
dev-fixtures
# Real device frames captured for offline OCR iteration (dev-only).
captures

# Video frames extracted for the accuracy benchmark (derived from the committed .mp4).
data/raw/stickers/frames
